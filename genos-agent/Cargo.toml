[package]
name = "genos-agent"
version = "0.1.0"
edition = "2021"

[dependencies]
genos-hal = { path = "../genos-hal" }
genos-kernel = { path = "../genos-kernel" }
genos-tools = { path = "../genos-tools" }
