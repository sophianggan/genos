[package]
name = "genos-tools"
version = "0.1.0"
edition = "2021"

[dependencies]
genos-hal = { path = "../genos-hal" }
genos-kernel = { path = "../genos-kernel" }
genos-mcp = { path = "../genos-mcp" }

[features]
default = []
hosted = ["genos-hal/hosted", "genos-kernel/std", "genos-mcp/hosted"]
